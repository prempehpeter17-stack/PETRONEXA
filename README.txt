PetroNexa brand assets.

- petronexa_logo.png: approved master logo supplied by the product owner.
- logo.png at the project root uses the same approved artwork for Streamlit and PDF reports.
- logo.svg is an SVG wrapper around the same approved artwork.

Create separate icon/splash variants from this master for production store packaging when platform-specific icon geometry is finalised.
